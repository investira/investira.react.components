import React from 'react';
import { default as WFormControl } from '@material-ui/core/FormControl';

function FormControl(props) {
    return <WFormControl {...props} />;
}

export default FormControl;
